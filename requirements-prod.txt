# Requirements production — sans sentence-transformers ni PyTorch
# En production, les embeddings passent par l'API HuggingFace (HF_TOKEN requis).

# LLM & RAG
langchain==1.3.4
langchain-anthropic>=1.4.4
langchain-community==0.4.2
langchain-core
langchain-text-splitters

# Vector store
faiss-cpu==1.14.2

# Parsing de documents
PyMuPDF==1.27.2.3
python-docx==1.2.0

# Backend
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.30
pydantic==2.13.4
pydantic-settings==2.14.1

# Stockage externe — REQUIS pour la persistance R2 sur Render
boto3==1.43.19

# Embeddings ONNX — sans PyTorch, compatible Render free (512 MB)
fastembed>=0.8.0

# Utilitaires
python-dotenv==1.2.2
tqdm==4.67.3
